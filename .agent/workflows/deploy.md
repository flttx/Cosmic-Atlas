---
description: Deploy the Cosmic Atlas application to a production environment
---

1. Run linting to ensure code quality:
   `npm run lint`

2. Run tests to verify logic:
   `npm run test`

3. Build the production bundle:
   `npm run build`

4. (Optional) Export a specific scene snapshot for gallery showcasing via the HUD export tool.
